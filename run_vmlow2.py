"""vmem 작은 lambda 쪽 — lambda 값을 벌린 사다리 (26-09-02 등록)

`run_vmlow.py` 는 1.5e-7 x3, 2e-7 x2 로 **반복**에 표본을 썼다. 반복 대신
**서로 다른 lambda 로 곡선을 넓게 덮는다**는 방침으로 바꾼다.
중복이던 l15_r2 / l15_r3 / l20_r3 는 70에폭에서 중단했고,
l15_r1(1.5e-7) 과 l20_r2(2e-7) 는 그대로 완주시킨다 (GPU 0, 2).

**남은 GPU 1, 3, 5 에 새 lambda 세 개.** 착지점은 vmem 상단 실측 지수 0.253 으로 외삽
(2e-7 -> 50,296 과 3e-7 -> 45,396 에서 뽑음. 깊은 쪽 0.394 보다 완만하다):

    lambda=1e-7  -> ~60,000      1-s 5e-8 의 60,215 자리
    lambda=7e-8  -> ~65,600      빈 구간
    lambda=5e-8  -> ~71,400      규제 없음(78,789) 바로 아래

이러면 vmem 곡선이 로그 lambda 축에서 고른 사다리가 된다:
    5e-8(71K) - 7e-8(66K) - 1e-7(60K) - 1.5e-7(54K) - 2e-7(50K) - 3e-7(45K, n=5)

**이 구간을 보는 이유** — 규제가 약해질수록 baseline(95.00%) 으로 수렴해야 한다.
수렴하지 않고 봉우리가 있으면 규제가 일반화에 기여한다는 뜻이고, 그게 "baseline 보다
높으면서 스파이크가 적은 점"의 유일한 후보다. 지금 곡선 꼭대기는 1-s 5e-8 의
95.03% @ 60,215 (n=1) 하나뿐이라 봉우리가 있는지 없는지 말할 수 없다.

RESERVED 로 GPU 0(l15_r1), 2(l20_r2), 4, 6, 7 을 막아 다른 런과 충돌하지 않게 한다
(08-30 에 런처 둘이 같은 GPU 를 잡아 런 하나가 죽은 적이 있다).
저장은 `_vmlow/` 로 같이 모으고, 접두사도 vlo- 그대로다.
"""
import run_vmlow as R

# GPU 0 = l15_r1 진행 중, 2 = l20_r2 진행 중, 4 = 다른 프로세스, 6·7 = juyun 님
R.RESERVED = {0, 2, 4, 6, 7}

#          이름     lambda      예상 착지
R.JOBS = [
    ('l10', '1E-7'),    # ~60,000
    ('l07', '7E-8'),    # ~65,600
    ('l05', '5E-8'),    # ~71,400
]

if __name__ == '__main__':
    R.main()
